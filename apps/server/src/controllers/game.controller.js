//import gameService from '../services/game.service.js';

class GameController {
  async getAvailableGames(req, res, next) {
    try {
      //getAvailableGames
    } catch (error) {
      next(error);
    }
  }

  async createGame(areq, res, next) {
    try {
    } catch (error) {
      next(error);
    }
  }

  async getGameById(req, res, next) {
    try {
    } catch (error) {
      next(error);
    }
  }

  async getGameState(req, res, next) {
    try {
    } catch (error) {
      next(error);
    }
  }

  getGameHistory(req, res, next) {
    try {
    } catch (error) {
      next(error);
    }
  }
}

export default new GameController();
